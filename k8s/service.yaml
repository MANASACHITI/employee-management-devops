apiVersion: v1
kind: Service
metadata:
  name: ems-service
spec:
  selector:
    app: employee-management
  type: NodePort
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 9090
      nodePort: 30001